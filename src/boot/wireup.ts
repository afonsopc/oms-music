/**
 * Boot wiring. Scaffolded as an inert no-op by WP1; owned by WP12 afterwards.
 * WP12 fills this with side-effect imports of every subsystem's register.ts
 * (downloads, remote, jam, social, separation) in dependency-safe order.
 */
export {};
