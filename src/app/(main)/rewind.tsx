/** Rewind em stories (pedido do dono, 2026-08-18): irmão das tabs como a
 *  gallery, para viver em ecrã inteiro por cima do navegador. */
import RewindScreen from "@/features/rewind";

export default RewindScreen;
