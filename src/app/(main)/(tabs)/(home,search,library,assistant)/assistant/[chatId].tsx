/** Uma conversa do assistente (dono, 2026-08-16): empurrada por cima da tab,
 *  `chatId` numerico abre a sessao guardada, "new" comeca uma em branco. */
import AssistantChatScreen from "@/features/assistant/Chat";

export default AssistantChatScreen;
