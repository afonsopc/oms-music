/**
 * The ONE shared placeholder artwork asset (FR-21). Every artwork chain ends
 * here - never a letter tile or icon (initials avatars are legal only for
 * pictureless artists in card grids).
 */
export const PLACEHOLDER_ARTWORK = require("../../assets/images/fallbackPhoto.jpg") as number;
