# Pedidos do dono por mensagem (sessão cloud 2026-08-17/18)

Chegaram a meio da sessão, com screenshots do Spotify. O que já ficou
feito está marcado; o resto é a fila da próxima sessão, POR ORDEM.

## 1. Header à Spotify em todas as tabs (screenshots: Home/Procurar/Biblioteca)

"Avatar sempre à esquerda, muda só o que está à direita." Hoje a Home tem
o wordmark à esquerda e o avatar à direita - o pedido novo INVERTE isso.
Fazer um `TabHeader` partilhado: avatar (abre a conta) + conteúdo à
direita - na Home as FilterPills, na Pesquisa o título "Procurar", na
Biblioteca "A tua Biblioteca" + acções. Nota: o wordmark Druk Wide da Home
(pedido de 2026-08-16) sai da Home com isto; confirmar com o dono se o
querem noutro sítio (splash? definições?).

## 2. Rewind à Spotify Wrapped, formato stories do Instagram

**Estado: bloqueado de propósito pelo 2.4** - primeiro a auditoria das
contagens e o `source`/`listened_s` (proposta com diff pronta), depois a
vitrina; um Rewind bonito sobre números errados é pior do que nenhum.
O formato: ecrã inteiro, cartões que avançam por toque (esq/dir), barra de
progresso segmentada no topo, gestos de pause em long-press. Componente
`StoryPager` genérico (verificável no Chromium!) + cartões de conteúdo
(top artista com foto, top música, minutos, gráfico por mês). O backend
precisa de um endpoint `GET /rewind/:year` com os agregados - especificar
quando a auditoria fechar.

## 3. Stories de preview de artistas

O mesmo `StoryPager` reutilizado: na página do artista (ou num rail da
Home), um modo "preview" que passa excertos das top músicas (30 s, com o
próprio motor - a app já sabe tocar por posição) com a foto do artista em
fundo. Fazer DEPOIS do StoryPager do Rewind existir.

## 4. Recomendações de rádios na Home

Rail "Rádios para ti": tiles `MixTileArtwork` com ícone radio, seeds dos
top artistas 30d (`artistRadioRoute`) e das top músicas
(`songRadioRoute`). Só cliente, dados já existem (useTopArtists/topSongs).
Pequeno e verificável - bom primeiro item de sessão.

## 5. "This Is <artista>"

**Feito nesta sessão**: gerador (diff testado em
docs/propostas/2026-08-17-mixes-novos.md) + cliente pronto (kinds,
gradientes, catálogos). Falta só aplicar o diff no backend.

## 6. Página de perfil (screenshot)

Referência do Spotify: hero com avatar grande à esquerda e nome +
"seguidores/a seguir" à direita; linha de acções (Editar/partilhar/
definições) SOB o hero; secções "Playlists" (com Gerir), "Artistas
reproduzidos recentemente" com followers. O nosso perfil já tem top
artistas/músicas/recentes - o trabalho é sobretudo de layout do hero e da
linha de acções. Juntar ao redesenho pendente da página de artista (pontos
15/16 antigos) numa passagem de desenho única.

## Também pendente das mensagens anteriores

- Botão de sync diário na página do artista (backend PRONTO em
  docs/propostas/2026-08-17-artist-sync-diario.md).
- Ecrã de chat do assistente (backend PRONTO em
  docs/propostas/2026-08-17-assistente.md).
