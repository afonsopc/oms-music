{
  "name": "oms-music-desktop",
  "private": true,
  "version": "1.1.0",
  "description": "Shell Tauri v2 do plano 'uma so app' (F5): o export web de oms-music dentro de uma janela nativa. Este package.json e o UNICO sitio JS onde o desktop pode declarar dependencias novas - o package.json da app fica intocado.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "bash scripts/build-macos.sh",
    "build:linux": "bash scripts/build-linux.sh",
    "bindings": "bash scripts/export-bindings.sh",
    "keys": "bash scripts/generate-updater-keys.sh"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/cli": "^2"
  }
}
